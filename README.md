# AskTube-AI
This app utilizes Generative AI, employing an open-source LLM and RAG to enhance functionality and output quality. The application extracts subtitles from a provided YouTube video URL, converts them into numerical embeddings, and stores the vectors in a vector store/database. It then processes user queries using the LLM to provide relevant answers.🤖
